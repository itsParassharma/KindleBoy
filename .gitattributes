# Normalise line endings to LF in the repo. The Kindle shell script and the
# whole toolchain are POSIX, so CRLF would break run.sh.
* text=auto eol=lf
*.sh    text eol=lf
*.c     text eol=lf
*.h     text eol=lf
Makefile text eol=lf
